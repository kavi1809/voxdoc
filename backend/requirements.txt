# ── Web framework ──────────────────────────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-multipart==0.0.32          # file uploads (>=0.0.18 fixes CVE-2024-53981)
slowapi==0.1.10                   # rate limiting on auth + upload endpoints

# ── Gemini ─────────────────────────────────────────────────────────────────────
# The old `google-generativeai` SDK was end-of-lifed on 2025-11-30.
# `google-genai` is the unified replacement (client-based, has native async).
google-genai==2.20.0

# ── LangChain / LangGraph — agent loop and tool orchestration ──────────────────
langgraph==1.2.11
langchain==1.3.18
langchain-google-genai==4.3.7     # 4.x is built on google-genai (1.x used the dead SDK)

# ── Retrieval ──────────────────────────────────────────────────────────────────
chromadb==1.5.9                   # vector store, persisted to disk
rank-bm25==0.2.2                  # keyword half of hybrid search
# fastembed runs embeddings LOCALLY via ONNX Runtime — no torch, no API calls.
# This is what removes the per-chunk Gemini embedding round-trips.
fastembed==0.8.0

# ── Document parsing ───────────────────────────────────────────────────────────
pypdf==6.16.2
python-docx==1.2.0
pandas==3.0.5
openpyxl==3.1.5                   # pandas needs this to read .xlsx

# ── Web scraping (URL ingestion) ───────────────────────────────────────────────
httpx==0.28.1
beautifulsoup4==4.15.0

# ── Auth ───────────────────────────────────────────────────────────────────────
# PyJWT rather than python-jose: actively maintained, smaller surface.
# bcrypt used directly rather than via passlib, which crashes on bcrypt>=4.1
# with `AttributeError: module 'bcrypt' has no attribute '__about__'`.
PyJWT==2.11.0
bcrypt==5.0.0

# ── Database ───────────────────────────────────────────────────────────────────
sqlalchemy==2.0.52

# ── Settings ───────────────────────────────────────────────────────────────────
pydantic-settings==2.15.0

# ── Tests ──────────────────────────────────────────────────────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
